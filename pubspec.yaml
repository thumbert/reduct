name: reduct
description: A Dart package to generate polyglot code from a DuckDB table definition.
version: 0.3.1
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:


dev_dependencies:
  lints: ^6.1.0
  test: ^1.29.0
